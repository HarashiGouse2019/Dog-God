using UnityEngine;

[CreateAssetMenu(fileName = "Great Shield", menuName = "Great Shield")]
public class GreatShield : Weapon
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
