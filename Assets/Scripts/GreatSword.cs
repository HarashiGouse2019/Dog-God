using UnityEngine;

[CreateAssetMenu(fileName = "New Great Sword", menuName = "Great Sword")]
public class GreatSword : Weapon
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
