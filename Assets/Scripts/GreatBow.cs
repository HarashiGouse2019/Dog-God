using UnityEngine;

[CreateAssetMenu(fileName = "New Great Bow", menuName = "Great Bow")]
public class GreatBow : Weapon
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
